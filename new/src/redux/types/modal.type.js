import keyMirror from "keymirror";

export const modalTypes = keyMirror({
	HIDE_MODAL: null,
	SHOW_MODAL: null,
});

export const MODAL_TEMPLATE_MAP = keyMirror({
    LOGIN: null,
    REGISTER: null
});
